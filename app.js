const names = ['Paola', 'Jenny', 'Susana', 'Rocio', 'Inmaculada'];
const capitalNames = [];


for (let i = 0; i < names.length; i++){
    const capitalNames = names[i] .toUpperCase();
    capitalNames.push(capitalNames);
}

console.log(capitalNames);